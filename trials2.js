// 4. creat an IIFE that takes in a message as soon as the page loads. let it output the area of a rectangle when page loads.
// hint: area of rectangle = length * width.

// ((length, width) => {
//   const area = length * width;
//   const output = `The rectangle with the lenth ${length}cm and width ${width}cm, has an area of ${area}cm`;

//   console.log(output);
// })(4, 5);